<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    <link rel="stylesheet" href="style.css">
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.2/gsap.min.js"></script>
</head>
<body>
    
    <header>
        <div class="navbar">
            <h1 class="site-title">Space & Exoplanet Exploration</h1>
            <nav>
                <ul>
                    <li><a href="index.html">Home</a></li>
                    <li><a href="exoplanets.html">Exoplanets</a></li>
                    <li><a href="login.html">Login</a></li>
                    <li><a href="signup.html">Sign Up</a></li>
                    <li><a href="videoresources.html">Video Resources</a></li>
                    <li><a href="quiz.html">Quiz</a></li>
                    <li><a href="spacevisualization.html">Space Visualization</a></li>
                    <li><a href="index.html">Canvas</a></li>
                </ul>
            </nav>
        </div>
    </header>

 
    <section class="hero">
        <div class="hero-content">
            <h2>Explore the Mysteries of the Universe</h2>
            <p>Join us on an exciting journey through the cosmos and learn about the fascinating worlds beyond our solar system.</p>
            <a href="signup.html" class="btn-primary">Get Started</a>
        </div>
        <div class="hero-image">
            <video src="" alt="Space Background">
        </div>
    </section>

    <section class="info-cards">
        <div class="card" id="card-1">
            <h3>What are Exoplanets?</h3>
            <p>Exoplanets are planets that orbit stars outside our solar system. They can be gas giants like Jupiter or rocky like Earth.</p>
            <a href="exoplanets.html" class="btn-secondary">Learn More</a>
        </div>
        <div class="card" id="card-2">
            <h3>Exoplanet Exploration</h3>
            <p>Scientists have discovered thousands of exoplanets using space telescopes like Kepler and the James Webb Space Telescope.</p>
            <a href="exoplanets2.html" class="btn-secondary">Explore Now</a>
        </div>
        <div class="card" id="card-3">
            <h3>Join the Community</h3>
            <p>Get involved in the Space Apps Challenge and contribute to the growing body of knowledge on exoplanets.</p>
            <a href="signup.html" class="btn-secondary">Sign Up</a>
        </div>
    </section>
    <embed src=" https://eyes.nasa.gov/apps/exo/#/"height="500px" width="1519px" >

    
    <footer>
        <p>&copy; 2024 Space Exploration. All Rights Reserved.</p>
    </footer>
    

    <script>
        
        gsap.from(".hero-content", { opacity: 0, y: -50, duration: 1.5, delay: 0.5 });
        gsap.from(".info-cards .card", { opacity: 0, y: 30, duration: 1.5, delay: 0.3, stagger: 0.2 });
    </script>
    <h2>Sign Up</h2>
    <form method="post" action="auth.php">
        <input type="text" name="username" placeholder="Username" required><br>
        <input type="email" name="email" placeholder="Email" required><br>
        <input type="password" name="password" placeholder="Password" required><br>
        <input type="submit" name="signup" value="Sign Up">
    </form>

    <h2>Login</h2>
    <form method="post" action="auth.php">
        <input type="text" name="username" placeholder="Username" required><br>
        <input type="password" name="password" placeholder="Password" required><br>
        <input type="submit" name="login" value="Login">
    </form>
</body>
</html>
<!-- auth.php -->
<?php
// Database connection
$host = "localhost";
$username = "your_username";
$password = "your_password";
$database = "your_database";

$conn = new mysqli($host, $username, $password, $database);

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

// Function to sanitize user input
function sanitize_input($data) {
    $data = trim($data);
    $data = stripslashes($data);
    $data = htmlspecialchars($data);
    return $data;
}

// Handle user signup
if (isset($_POST['signup'])) {
    $username = sanitize_input($_POST['username']);
    $email = sanitize_input($_POST['email']);
    $password = password_hash($_POST['password'], PASSWORD_DEFAULT);

    $sql = "INSERT INTO users (username, email, password) VALUES (?, ?, ?)";
    $stmt = $conn->prepare($sql);
    $stmt->bind_param("sss", $username, $email, $password);

    if ($stmt->execute()) {
        echo "User registered successfully";
    } else {
        echo "Error: " . $stmt->error;
    }

    $stmt->close();
}

// Handle user login
if (isset($_POST['login'])) {
    $username = sanitize_input($_POST['username']);
    $password = $_POST['password'];

    $sql = "SELECT id, username, password FROM users WHERE username = ?";
    $stmt = $conn->prepare($sql);
    $stmt->bind_param("s", $username);
    $stmt->execute();
    $result = $stmt->get_result();

    if ($result->num_rows === 1) {
        $user = $result->fetch_assoc();
        if (password_verify($password, $user['password'])) {
            session_start();
            $_SESSION['user_id'] = $user['id'];
            $_SESSION['username'] = $user['username'];
            echo "Login successful";
        } else {
            echo "Invalid username or password";
        }
    } else {
        echo "Invalid username or password";
    }

    $stmt->close();
}

$conn->close();

// Redirect back to the HTML page after processing
header("Location: index.html");
exit();
?>